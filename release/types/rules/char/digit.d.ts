import { Range } from './range';
import { Rule } from '../../rule';
/**
 * Character digit, rule class.
 */
export declare class Digit extends Range implements Rule {
    /**
     * Default constructor.
     */
    constructor();
}
